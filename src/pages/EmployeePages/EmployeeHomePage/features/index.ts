/**
 * Feature-level surface for Employee Home.
 * Add named workflows here when logic outgrows a single hook (e.g. multi-step onboarding).
 */
export type { EmployeeHomeViewModel } from "../types/employeeHome.types";

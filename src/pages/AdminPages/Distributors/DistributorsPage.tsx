/**
 * Admin route shell for `/distributors`.
 * Screen implementation: `src/components/Distributors/DistributorsPage.tsx`.
 */
import DistributorsPage from "../../../components/Distributors/DistributorsPage";

export default function DistributorsRoutePage() {
  return <DistributorsPage />;
}
